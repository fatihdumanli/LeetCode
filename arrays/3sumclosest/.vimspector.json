{
  "configurations": {
    "run-delve": {
      "adapter": "delve",
      "configuration": {
        "request": "launch",
        "env": { "GO111MODULE": "off" },
        "mode": "debug", 
        "program": "${workspaceRoot}/solution.go"
      }
    }
  }
}
